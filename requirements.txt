# -----------------------------------------------------------------
# Python Dependencies for the Pepper VR Teleoperation Server
# Updated: October 2025 - Now using qi framework instead of naoqi
# To install: pip install -r requirements.txt
# -----------------------------------------------------------------

# Core library for Pepper robot communication (qi framework)
qi==3.1.5

# WebSocket library for real-time commands from Unity
websockets==13.0

# Web framework for streaming the camera feed
Flask==3.1.2

# Flask extension for handling Cross-Origin Resource Sharing
Flask-Cors==6.0.1

# Fundamental library for numerical operations (image data)
numpy==1.26.4

# Computer vision library for processing camera frames
opencv-python==4.12.0.88

# Keyboard input library for the test controller
pynput==1.7.6